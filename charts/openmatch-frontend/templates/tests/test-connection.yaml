apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openmatch-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "openmatch-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openmatch-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
